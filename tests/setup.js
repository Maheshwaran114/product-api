/**
 * Global test setup file
 * This file runs before all tests
 */

// Set NODE_ENV to 'test' to enable test-specific behavior
process.env.NODE_ENV = 'test';

// Setup any other global test configurations here